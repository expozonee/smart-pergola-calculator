import { getPrice } from "@/utils/getPrice";

export async function Price() {
  // const price = getPrice(width,height)

  return (
    <div>
      <h2 className="text-white">price</h2>
    </div>
  );
}
